// Generated by CoffeeScript 1.3.1
(function() {

  $(function() {
    var MyBackboneCollection;
    MyBackboneCollection = Backbone.Collection.extend({
      sync: function(method, model, options) {
        var params;
        if (typeof this.type === "undefined") {
          this.type = "POST";
        }
        if (typeof this.url === "function") {
          options.url = this.url();
        } else {
          options.url = this.url;
        }
        if (!this.noJsonStringify) {
          options.data = {
            request: JSON.stringify(this.query)
          };
        } else {
          options.data = this.query;
        }
        params = {
          type: this.type,
          dataType: 'json'
        };
        return $.ajax(_.extend(params, options));
      }
    });
    return window.RecipeSearchCollection = MyBackboneCollection.extend({
      model: RecipeModel,
      query: {
        searchstring: "",
        limit: 10,
        offset: 0
      },
      url: "/api/ios/search_recipe/"
    });
  });

}).call(this);
