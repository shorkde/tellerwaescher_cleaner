// Generated by CoffeeScript 1.3.1
(function() {

  $(function() {
    return window.Admin = Backbone.Router.extend({
      routes: {
        "": "main",
        "ingredient/:id": "ingr"
      },
      initialize: function() {
        Backbone.history.start({
          pushState: false
        });
        return $('#btn-back').live('click', function() {
          console.log("back clicked");
          return window.history.back();
        });
      },
      main: function() {
        console.log("main");
        this.searchView = new SearchView({
          collection: new IngredientsCollection()
        });
        return $('#ingr_list').html(this.searchView.render().el);
      },
      ingr: function(id) {
        console.log("ingr" + id);
        this.recipeDetailView = new IngrDetailView({});
        return $('#container').html(this.recipeDetailView.render().el);
      },
      map: function(id) {
        return console.log("map" + id);
      }
    });
  });

}).call(this);
